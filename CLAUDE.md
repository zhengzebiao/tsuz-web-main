# 项目协作规则

## 实施方案与阶段开发文档

### 1. 适用范围

当任务包含以下任一意图时，必须遵循本节规则：

- 为项目功能提供方案、设计方案、实施方案或分阶段计划；
- 根据某个 Markdown 方案文档开发“第 N 阶段”或某个明确命名的阶段；
- 继续、补充、验收或记录一个已经分阶段实施的功能。

### 2. 文档分层

项目中的方案文档分为三类，职责不能混用：

1. **总实施方案**：记录完整业务目标、已确认决策、总体设计、阶段边界、总体验收和部署要求，是该功能的方案基准。
2. **阶段实现计划**：把总方案中的一个阶段展开为可执行的修改范围、技术设计、实施步骤和验收标准，是当前阶段的实施契约。
3. **阶段执行记录**：记录阶段实际完成内容、真实改动、验证命令与结果、计划偏差、遗留问题和阶段结论，是实施事实记录。

通用模板：

- [总实施方案模板](plan/IMPLEMENTATION_PLAN_TEMPLATE.md)
- [阶段实现计划模板](plan/IMPLEMENTATION_PHASE_PLAN_TEMPLATE.md)
- [阶段执行记录模板](plan/IMPLEMENTATION_PHASE_EXECUTION_TEMPLATE.md)

完整实例参考：

- [邮箱注册总实施方案](plan/EMAIL_REGISTRATION_SES_IMPLEMENTATION_PLAN.md)
- [邮箱注册第二阶段实现计划](plan/EMAIL_REGISTRATION_SES_IMPLEMENTATION_PHASE_2_PLAN.md)
- [邮箱注册第二阶段执行记录](plan/EMAIL_REGISTRATION_SES_IMPLEMENTATION_PHASE_2_EXECUTION.md)

模板决定通用结构和最低内容要求；具体方案文档决定业务事实和已确认约束。实例只用于参考详细程度、表达方式和三类文档的对应关系，不得把其中的腾讯云 SES、邮箱、模板 ID 等业务信息复制到无关方案中。

### 3. 信息优先级

编写方案或实施阶段任务时，按以下优先级处理信息：

1. 用户当前明确提出或确认的要求；
2. 用户指定的总实施方案及其已确认决策；
3. 当前阶段实现计划；
4. 已有阶段执行记录；
5. 当前代码、配置、迁移、测试和部署文件所反映的仓库事实；
6. 通用模板和实例文档。

若文档与当前代码不一致，必须先查明原因并在计划或执行记录中说明；不能静默选择其中一方。不得擅自改变总方案中的已确认业务决策。只有会实质改变范围、接口、数据兼容性、安全性或验收结果的歧义才需要向用户确认，常规实现细节应结合仓库惯例自行判断。

### 4. 提供新方案时

当用户要求“提供某个方案”时：

1. 先检查当前仓库架构、已有实现、测试、配置和相关文档，不凭模板假设仓库事实；
2. 使用 [总实施方案模板](plan/IMPLEMENTATION_PLAN_TEMPLATE.md) 组织内容；
3. 参考邮箱注册总方案的完整度，但根据实际功能增删不适用小节；
4. 明确区分已确认决策、合理默认值、待确认项和非目标；
5. 必须覆盖目标、现状适配、接口或契约、数据与状态、服务拆分、文件清单、异常与安全、测试验收、部署回滚及阶段顺序；
6. 每个阶段必须写明开发内容、本阶段不实现、前置依赖、验收标准和交付文档，避免阶段范围重叠；
7. 未经用户确认的外部资源、凭证、生产数据或业务参数不能写成“已就绪”。

### 5. 根据方案开发某一阶段时

当用户要求“根据 `xx.md` 开发第 N 阶段”或“开发某命名阶段”时，执行以下流程：

#### 5.1 确定阶段基准

1. 阅读用户指定的总方案全文，并定位目标阶段的目标、开发内容、不实现内容和验收标准；
2. 阅读该功能已有的阶段计划、阶段执行记录以及总方案引用的相关文档；
3. 检查当前代码与 Git 状态，确认前置阶段是否已经落地，以及是否存在用户未说明的工作区改动；
4. 以总方案为基准创建或更新当前阶段实现计划；若计划已经存在，则核对其与总方案及当前代码是否一致；
5. 阶段实现计划明确后再编码。需要审批时，遵循当前会话的计划审批机制。

#### 5.2 严格控制阶段范围

- 只实现当前阶段列出的能力，不提前加入后续阶段业务功能；
- 为完成当前阶段所必需的最小兼容性修改可以实施，但必须在阶段计划和执行记录中说明原因；
- 不得因为后续阶段更方便而扩大公共接口、迁移数据或引入额外依赖；
- 若发现总方案不可实现或存在安全、数据损坏、兼容性风险，应明确指出，同时完成所有不受阻塞的当前阶段内容；
- 代码、测试和文档都必须遵循仓库现有命名、结构和风格。

#### 5.3 实施与验证

1. 按阶段计划逐项实现，并同步补充相应测试；
2. 优先执行与改动直接相关的定向测试，再执行计划要求的 lint、类型检查、迁移检查、锁文件检查和全量测试；
3. 所有验证结果必须如实记录：通过、失败、因环境受限未执行三者不能混淆；
4. 外部服务真实调用、生产迁移、部署、发送消息等具有副作用的验收，仅在用户明确授权且环境就绪时执行；否则记录为待执行，不得推断为通过；
5. 阶段验收未全部满足时不得把阶段标记为“已完成”，应标记为“部分完成”或“受阻”，并列出剩余项。

### 6. 阶段完成后的强制文档同步

每次阶段开发完成后，必须同步维护以下三份记录，而不是只修改代码：

1. **总实施方案**
   - 更新当前阶段状态和验收结论；
   - 链接对应的阶段计划与执行记录；
   - 记录经确认的方案变更或对后续阶段有影响的事实；
   - 不把详细代码流水账重复写入总方案，也不改写仍然有效的原始业务决策。

2. **当前阶段实现计划**
   - 保留阶段目标、范围和实施步骤；
   - 更新文档状态；
   - 记录实施过程中确认的设计调整及其原因；
   - 保证最终计划与实际实施契约一致，但不要用它替代执行记录。

3. **当前阶段执行记录**
   - 使用 [阶段执行记录模板](plan/IMPLEMENTATION_PHASE_EXECUTION_TEMPLATE.md) 创建或更新；
   - 列出实际修改文件、关键行为、未实现内容、验证命令及真实结果；
   - 逐项映射阶段验收标准，说明通过证据；
   - 记录计划偏差、遗留问题、环境限制和下一阶段入口。

开发第几阶段，就维护第几阶段对应的文档。不得固定覆盖第二阶段文档，也不得用新阶段记录覆盖旧阶段历史。

### 7. 文件命名

若现有功能已经形成稳定命名规则，继续沿用。没有既有规则时，默认使用：

```text
plan/<FEATURE>_IMPLEMENTATION_PLAN.md
plan/<FEATURE>_IMPLEMENTATION_PHASE_<N>_PLAN.md
plan/<FEATURE>_IMPLEMENTATION_PHASE_<N>_EXECUTION.md
```

其中 `<FEATURE>` 使用可读的英文大写下划线名称，`<N>` 使用阿拉伯数字。总方案、阶段计划和执行记录之间必须使用相对 Markdown 链接互相引用。

### 8. 文档质量要求

- 使用与用户和现有方案一致的语言；本项目现有方案默认使用中文；
- 所有文件路径、接口、命令、配置名和验收结果必须能够在仓库中追溯；
- “计划新增”和“实际已新增”、“建议执行”和“已经执行”必须明确区分；
- 不伪造测试结果、生产状态、外部服务状态、数据库内容或用户确认；
- 不在方案、计划或执行记录中写入密码、Token、验证码、Secret 或其他敏感值；
- 已有专门文档可以链接引用，避免无价值地复制大段内容，但文档本身必须包含理解当前阶段所需的关键结论；
- 模板中的占位符、填写说明和不适用章节必须在正式文档中替换或删除；
- 阶段完成标准包括代码、测试和上述文档同步；若用户明确只要求规划或文档工作，则按其要求限定范围。
