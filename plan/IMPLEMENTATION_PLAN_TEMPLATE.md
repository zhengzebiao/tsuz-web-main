# {{功能名称}}实施方案

> 状态：{{草案 / 待确认 / 已确认，可进入实现 / 实施中 / 已完成}}
>
> 本方案基于 {{当前项目、技术栈和相关架构}}。事实来源包括 {{需求、代码、现有文档或外部契约}}。
>
> 相关文档：{{需求文档、设计文档、接口文档等相对链接；没有则删除本行}}

> 模板使用说明：正式方案必须替换所有 `{{占位符}}`，删除填写说明及不适用章节。先核对仓库事实，再填写本模板；不要把参考实例中的业务参数带入新方案。

## 1. 已确认业务配置与关键决策

使用表格集中记录已经由用户、产品要求、外部契约或仓库现状确认的事实。尚未确认的内容必须标记为“待确认”，不能写成既定事实。

| 项目 | 决策或配置 | 状态/来源 |
| --- | --- | --- |
| {{示例：业务流程}} | {{具体决策}} | {{已确认；来源}} |
| {{示例：兼容策略}} | {{具体决策}} | {{已确认；来源}} |
| {{示例：外部依赖}} | {{具体约束}} | {{待确认；负责人或阻塞点}} |

## 2. 背景与现状

### 2.1 背景

说明为什么需要本功能、要解决的问题、目标用户及业务上下文。

### 2.2 当前架构

列出与方案直接相关的现有模块、数据模型、接口、基础设施和测试，并用相对链接指向实际文件：

- {{现有入口或 API}}；
- {{现有服务或领域逻辑}}；
- {{现有模型、迁移或状态存储}}；
- {{现有部署、配置或外部系统边界}}。

### 2.3 现状差距

说明当前能力与目标之间的差距，以及哪些已有能力可以复用、哪些必须修改或新增。

## 3. 目标与非目标

### 3.1 目标

本期完成：

1. {{可验证的目标一}}；
2. {{可验证的目标二}}；
3. {{兼容性或运维目标}}。

### 3.2 非目标

本期明确不实现：

- {{容易被误解为包含在本期的功能}}；
- {{未来阶段或另一个项目负责的能力}}；
- {{未经确认的扩展}}。

## 4. 需求与核心流程

### 4.1 参与者和使用场景

| 参与者 | 前置条件 | 操作 | 预期结果 |
| --- | --- | --- | --- |
| {{参与者}} | {{前置条件}} | {{核心动作}} | {{可观察结果}} |

### 4.2 正常流程

```text
{{输入或触发}}
  ↓
{{校验}}
  ↓
{{核心处理}}
  ↓
{{持久化或外部交互}}
  ↓
{{响应或后续状态}}
```

### 4.3 异常与边界流程

描述重复请求、并发、超时、部分失败、幂等、权限不足、资源不存在、状态冲突和回滚等边界行为。

## 5. 当前架构适配与总体设计

### 5.1 设计原则

- {{复用哪些现有能力}}；
- {{保持哪些兼容行为}}；
- {{事务、一致性或安全原则}}；
- {{为什么不采用某个看似可行的替代方案}}。

### 5.2 目标架构

```text
{{调用方}}
  ↓
{{入口层}}
  ↓
{{应用/领域服务}}
  ↓
{{数据、缓存、队列或外部服务}}
```

说明各层职责、依赖方向和边界，避免只给出文件列表。

### 5.3 兼容策略

说明对现有接口、客户端、历史数据、配置、部署版本和回滚窗口的影响；若不需要兼容，也说明理由。

## 6. 接口与外部契约设计

没有 HTTP/API 变更时，可改为“公共调用契约”或删除不适用小节，但必须描述对外可观察行为。

### 6.1 {{接口或操作名称}}

```http
{{METHOD}} {{PATH}}
Content-Type: application/json
```

请求：

```json
{
  "{{field}}": "{{value}}"
}
```

成功响应：

```json
{
  "{{field}}": "{{value}}"
}
```

处理规则：

1. {{输入标准化与校验}}；
2. {{权限或状态检查}}；
3. {{事务和副作用顺序}}；
4. {{响应语义}}。

### 6.2 契约兼容性

记录版本、字段可选性、废弃策略、旧调用方行为和错误响应兼容要求。

## 7. 数据模型、迁移与状态设计

### 7.1 数据模型

列出新增或修改的表、字段、索引、约束和关系：

```text
{{实体}}
├── {{字段：类型、可空性、默认值}}
└── {{约束或关系}}
```

### 7.2 迁移策略

说明：

- upgrade 步骤；
- 历史数据回填；
- 大表锁、执行时间和分批策略；
- 新旧版本并存时的 expand-contract 兼容性；
- downgrade 或前向修复策略；
- 执行前备份和验证方式。

### 7.3 缓存、队列或临时状态

如适用，定义 Key、字段、TTL、用途隔离、清理、幂等和原子性。不得在文档中写入真实敏感值。

### 7.4 事务与并发

明确事务边界、锁策略、唯一约束、并发冲突处理，以及数据库与外部副作用之间的一致性策略。

## 8. 模块与服务拆分

### `{{ServiceOrModuleA}}`

- {{职责}}；
- {{输入和输出}}；
- {{依赖和异常边界}}；
- {{明确不负责的事项}}。

### `{{ServiceOrModuleB}}`

- {{职责}}；
- {{与现有模块的复用关系}}；
- {{测试边界}}。

说明为什么采用该拆分，以及如何避免业务逻辑散落在路由、模型或外部 SDK 调用中。

## 9. 配置、依赖与外部服务

### 9.1 配置

```dotenv
{{CONFIG_NAME}}={{非敏感示例值}}
```

逐项说明默认值、必填条件、不同环境差异、验证规则和 Secret 注入方式。

### 9.2 依赖

列出新增、升级或移除的依赖及锁文件影响，并说明选择理由、许可证或运行环境限制（如适用）。

### 9.3 外部服务契约

记录 endpoint、地域、超时、重试、幂等、限额、认证方式、失败降级和联调边界。Secret 只能描述注入方式，不能记录真实值。

## 10. 代码变更清单

### 配置和依赖

- `{{path/to/config}}`：{{计划改动}}；
- `{{dependency-file}}`：{{计划改动}}。

### API、Schema 或公共契约

- `{{path/to/api}}`：{{计划改动}}；
- `{{path/to/schema}}`：{{计划改动}}。

### 服务和领域逻辑

- `{{path/to/service}}`：{{计划改动}}。

### 模型和迁移

- `{{path/to/model}}`：{{计划改动}}；
- `{{path/to/migration}}`：{{计划改动}}。

### 测试和文档

- `{{path/to/test}}`：{{覆盖范围}}；
- `{{path/to/document}}`：{{记录内容}}。

清单中的“新增”和“修改”应明确区分；正式实现后由阶段执行记录列出实际文件。

## 11. 异常处理与可观测性

### 11.1 异常响应或错误契约

| 场景 | 外部结果 | 内部处理 |
| --- | --- | --- |
| {{参数错误}} | {{状态码/异常类型}} | {{是否记录、是否回滚}} |
| {{状态冲突}} | {{状态码/异常类型}} | {{并发或幂等处理}} |
| {{依赖失败}} | {{状态码/异常类型}} | {{超时、重试或降级}} |

### 11.2 日志、指标与追踪

明确成功/失败日志、脱敏方式、关联 ID、关键指标和告警；禁止记录 Secret、密码、完整 Token 或其他敏感内容。

## 12. 安全与权限要求

至少评估：

1. 认证和授权边界；
2. 最小权限和服务凭证；
3. 输入校验与注入风险；
4. 敏感数据的存储、传输和日志脱敏；
5. 枚举、重放、暴力尝试、限流和滥用；
6. CSRF、CORS、SSRF 或文件处理风险（按功能适用）；
7. 并发、事务、幂等和越权访问；
8. 依赖及外部服务故障的 fail-open/fail-closed 选择；
9. 数据保留、删除和审计要求。

不适用的项目应说明原因，不要只写“安全”。

## 13. 测试与验收

### 13.1 单元测试

- {{核心成功路径}}；
- {{输入和状态边界}}；
- {{异常转换、日志脱敏或幂等}}。

### 13.2 集成与迁移测试

- {{数据库/缓存/队列真实契约}}；
- {{upgrade、数据回填及兼容性}}；
- {{并发、事务回滚或外部服务边界 Mock}}。

### 13.3 API 或端到端测试

- {{完整用户流程}}；
- {{旧行为回归}}；
- {{失败场景和权限边界}}。

### 13.4 阶段外真实验证

列出需要真实凭证、受控账号、测试环境或生产发布窗口才能执行的验证，并注明默认是否进入 CI。

## 14. 部署、迁移与回滚检查清单

### 配置与 Secret

- [ ] {{各环境配置已同步}}；
- [ ] {{Secret 通过安全渠道注入}}；
- [ ] {{日志和部署输出不泄露敏感信息}}。

### 数据与基础设施

- [ ] {{迁移已在测试环境验证}}；
- [ ] {{备份、容量、权限和回滚评估完成}}；
- [ ] {{缓存、队列或外部资源隔离正确}}。

### 应用与兼容性

- [ ] {{定向、全量和回归测试通过}}；
- [ ] {{新旧版本兼容窗口已验证}}；
- [ ] {{监控、告警、灰度和停止条件就绪}}。

### 回滚策略

说明应用镜像回滚、数据库前向修复或 downgrade、配置回退、外部副作用处理和回滚后验证步骤。

## 15. 分阶段实施顺序

本模块分为 {{N}} 个阶段。每个阶段完成并验证后再进入下一阶段，不跨阶段提前加入业务功能。

### 第一阶段：{{阶段名称}}

> 状态：{{未开始 / 实施中 / 部分完成 / 已完成}}
>
> 阶段计划：{{对应文档链接；未创建时标记“待创建”}}
>
> 执行记录：{{对应文档链接；未执行时标记“待创建”}}

前置依赖：

- {{依赖、确认项或环境条件}}。

开发内容：

1. {{可执行工作项}}；
2. {{测试或迁移工作项}}；
3. {{文档交付项}}。

本阶段不实现：

- {{属于后续阶段或明确排除的内容}}。

阶段验收：

- {{可测试、可观察的结果}}；
- {{兼容性或安全要求}}。

### 第二阶段：{{阶段名称}}

按第一阶段的结构继续填写。根据实际阶段数量增删小节，每个阶段都必须包含状态、前置依赖、开发内容、不实现内容、验收标准、阶段计划和执行记录链接。

## 16. 风险、待确认项与决策记录

### 16.1 风险

| 风险 | 影响 | 缓解措施 | 状态 |
| --- | --- | --- | --- |
| {{风险}} | {{影响}} | {{措施}} | {{开放/已缓解}} |

### 16.2 待确认项

| 问题 | 为什么需要确认 | 建议默认值 | 负责人/状态 |
| --- | --- | --- | --- |
| {{问题}} | {{会改变哪些实现}} | {{建议}} | {{待确认}} |

没有剩余待确认项时，明确写“当前没有阻塞实施的待确认项”。

### 16.3 方案决策记录

| 决策 | 原因 | 替代方案 | 确认来源 |
| --- | --- | --- | --- |
| {{决策}} | {{权衡}} | {{未采用方案及原因}} | {{来源}} |

## 17. 完成标准

用端到端链路描述最终状态：

```text
{{输入}}
  ↓
{{验证与业务处理}}
  ↓
{{数据或状态变更}}
  ↓
{{输出}}
```

方案整体完成至少要求：

- 所有阶段验收标准有真实验证证据；
- 旧接口、历史数据和部署兼容要求得到验证；
- 安全、日志、监控、迁移和回滚要求落实；
- 总方案、每阶段计划及每阶段执行记录互相链接且状态一致；
- 未执行的外部或生产验证被明确列为发布前事项，而不是标记为通过。
