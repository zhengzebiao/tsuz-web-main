# {{功能名称}}：第 {{N}} 阶段“{{阶段名称}}”执行记录

> 状态：{{实施中 / 部分完成 / 受阻 / 已完成}}
>
> 执行日期：{{YYYY-MM-DD}}
>
> 总实施方案：[{{总方案名称}}]({{TOTAL_PLAN_RELATIVE_LINK}})
>
> 阶段实现计划：[{{阶段计划名称}}]({{PHASE_PLAN_RELATIVE_LINK}})

> 模板使用说明：本文件只记录实际发生的实施事实。必须替换所有 `{{占位符}}`，删除说明和不适用章节；不能把计划事项写成已完成，也不能把未执行的检查写成通过。

## 1. 执行范围与结论

本次根据总方案完成第 {{N}} 阶段“{{阶段名称}}”。

阶段结论：{{一句话说明已完成、部分完成或受阻，以及能否进入下一阶段}}。

本阶段实际完成：

1. {{已经落地且可验证的能力}}；
2. {{已经落地的兼容、迁移或安全处理}}；
3. {{已经补充的测试和文档}}。

本阶段明确未实现或未执行：

- {{按阶段边界留给后续阶段的能力}}；
- {{因缺少环境、授权或外部资源未执行的验证}}；
- {{受阻事项；没有则写“无”}}。

## 2. 实际代码与配置变更

按职责分组记录实际变更。使用相对链接指向文件，并说明最终行为，不复制完整代码。

### 2.1 {{模块或能力一}}

- [{{file}}]({{RELATIVE_LINK}})：{{实际修改及最终行为}}；
- [{{test file}}]({{RELATIVE_LINK}})：{{实际覆盖场景}}。

关键实现：

```text
{{实际执行链路或状态变化}}
```

### 2.2 {{模块或能力二}}

- [{{file}}]({{RELATIVE_LINK}})：{{实际修改及最终行为}}。

说明：

- {{输入、输出和异常边界}}；
- {{事务、并发、幂等或兼容处理}}；
- {{复用的现有能力}}。

### 2.3 数据、迁移和状态

记录实际新增/修改的表、字段、索引、约束、数据回填、缓存 Key、TTL、队列或状态机行为。

若执行了迁移，记录：

- 迁移版本：`{{revision}}`；
- 执行环境：{{测试/本地/生产；不要记录凭证}}；
- upgrade 结果：{{真实结果}}；
- downgrade/前向修复验证：{{真实结果或未执行原因}}；
- 数据核对：{{行数、约束或抽样结论；不得编造}}。

不涉及时写明“不涉及数据结构或持久状态变更”。

### 2.4 API、Schema 或公共契约

列出实际新增或改变的接口、字段、状态码和兼容行为。不涉及时写明“本阶段未改变公共契约”。

### 2.5 配置、依赖和外部服务

- {{实际新增或修改的配置名及默认行为}}；
- {{依赖/锁文件变化}}；
- {{外部服务调用边界及是否真实调用}}；
- {{Secret 注入和脱敏结果，不记录真实 Secret}}。

## 3. 关键设计结果

记录实现后值得后续阶段依赖的最终约定：

1. {{最终公共方法、数据契约或状态语义}}；
2. {{错误处理、事务或并发语义}}；
3. {{兼容性和安全约定}}。

只记录能够从代码或验证结果证实的结论。

## 4. 与阶段计划的差异

没有差异时写“实现与阶段计划一致，无范围或设计偏差”。

| 差异 | 计划内容 | 实际实施 | 原因 | 影响与处理 |
| --- | --- | --- | --- | --- |
| {{差异}} | {{原计划}} | {{实际结果}} | {{仓库事实、风险或已确认变更}} | {{对验收/后续阶段影响}} |

任何影响总方案、公共契约、数据兼容或后续阶段边界的差异，都必须同步回总方案和阶段计划。

## 5. 测试与验证结果

### 5.1 验证汇总

| 检查 | 命令或方法 | 结果 | 证据/说明 |
| --- | --- | --- | --- |
| 定向测试 | `{{command}}` | {{通过/失败/未执行}} | {{通过数量、失败摘要或未执行原因}} |
| Lint/格式 | `{{command}}` | {{通过/失败/未执行}} | {{结果摘要}} |
| 类型/迁移/锁文件 | `{{command}}` | {{通过/失败/未执行}} | {{结果摘要}} |
| 全量测试 | `{{command}}` | {{通过/失败/未执行}} | {{通过数量、失败摘要或未执行原因}} |
| Diff 检查 | `{{command}}` | {{通过/失败/未执行}} | {{结果摘要}} |

### 5.2 失败与未执行项

逐项记录失败原因、是否由本次改动引入、已经完成的排查、剩余处理和复现命令。全部通过且没有跳过时写“无”。

- {{失败或未执行项}}：{{真实原因和影响}}。

### 5.3 真实环境或人工验证

| 验证项 | 环境 | 副作用/授权 | 结果 |
| --- | --- | --- | --- |
| {{外部服务、部署、真实数据库等}} | {{环境}} | {{是否已授权和实际发生}} | {{通过/失败/未执行及原因}} |

不得用 Mock 或单元测试结果替代真实环境验证结论。

## 6. 阶段验收结果

逐项映射阶段计划和总方案中的验收标准，不能合并后遗漏。

| 编号 | 验收标准 | 结果 | 验证证据 |
| --- | --- | --- | --- |
| AC-{{N}}-01 | {{原验收标准}} | {{通过/未通过/待环境验证}} | [{{测试或文件}}]({{RELATIVE_LINK}}) 或 `{{command}}` |
| AC-{{N}}-02 | {{原验收标准}} | {{通过/未通过/待环境验证}} | {{具体证据}} |

只有当前阶段所有必需验收项通过时，文档状态才能标记为“已完成”。仅发布前人工检查或明确属于后续阶段的验证，可以标记为“待环境验证”并在总方案部署清单中继续追踪。

## 7. 安全、兼容性与可观测性核对

### 安全

- {{权限和 fail-open/fail-closed 行为}}；
- {{敏感信息未进入代码、日志和响应的验证}}；
- {{限流、重放、枚举、输入校验或其他适用保护}}。

### 兼容性

- {{旧接口、历史数据、配置和部署版本的回归结果}}；
- {{新旧版本并存或回滚兼容性}}。

### 可观测性

- {{日志、指标、追踪和告警的实际变化}}；
- {{脱敏和关联 ID 行为}}。

未验证的内容必须明确标记，不得用“保持兼容”代替证据。

## 8. 遗留问题与后续阶段入口

### 8.1 当前阶段遗留问题

| 问题 | 影响 | 负责人/条件 | 处理阶段 |
| --- | --- | --- | --- |
| {{问题}} | {{影响}} | {{需要的权限、环境或决定}} | {{当前阶段补齐/发布前/第 N+1 阶段}} |

没有遗留问题时写“无”。

### 8.2 下一阶段可复用能力

- {{下一阶段可以直接调用的服务、契约或迁移}}；
- {{下一阶段必须继续遵守的安全或兼容约束}}；
- {{进入下一阶段前仍需满足的条件}}。

不得在本执行记录中把下一阶段计划内容表述为已经实现。

## 9. 文档同步记录

- [{{总方案名称}}]({{TOTAL_PLAN_RELATIVE_LINK}})：{{已更新的状态、链接、决策或后续影响}}；
- [{{阶段计划名称}}]({{PHASE_PLAN_RELATIVE_LINK}})：{{已更新的状态和设计调整}}；
- 本执行记录：{{记录范围和最终状态}}。

若任何一项尚未同步，本阶段不能标记为“已完成”。

## 10. 阶段结论

第 {{N}} 阶段{{已完成/部分完成/受阻}}：

- {{核心能力结论}}；
- {{测试和验收结论}}；
- {{安全与兼容结论}}；
- {{是否可以进入下一阶段；若不能，列出阻塞条件}}。
